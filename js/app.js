(function( $ ) { 'use strict';

document.addEventListener( 'DOMContentLoaded', onReady );
window.addEventListener( 'load', onLoad );

function onReady() {
  // script that runs when other scripts has loaded
}

function onLoad() {
  // script that runs when everything (including images) has loaded.
}

})( jQuery );